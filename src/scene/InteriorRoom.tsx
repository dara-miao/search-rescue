import type { InteriorDef } from '../game/interiors'

/** Stills live on the ROBOT pane as a photo, not as a WebGL plane. */
export function InteriorRoom(_props: { room: InteriorDef; thermal: boolean }) {
  return null
}
